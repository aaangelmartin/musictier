{
  "comment": "Editor-only config for Cloudflare Pages Functions. Not part of the npm build (tsc -b); Cloudflare compiles these at deploy time.",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "types": ["@cloudflare/workers-types"],
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true
  },
  "include": ["**/*.ts"]
}
